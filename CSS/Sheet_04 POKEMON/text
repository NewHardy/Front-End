picture=url.sprites.front_default
ID= url.ID
name=name
type=url.types.forEach.type.name
EVO= url.species.url.evolves_from_species.name

create Array
fetch main url{
Array {url1,url2}
}

array.forEach(element => 
    {
   getinfo(element)
});



getinfo
{
    const picture
    const ID
    const name
    const type

    fetch url
    {

    }
    .createCard
}
